mutation {
    createUserWithDefaultCollection(mail: "dave@example.org") {
        users {
            id
            mail
            defaultCollection {
                id
            }
        }
    }
}